
public class BumpmapEditor extends TextureHandler {

	BumpmapEditor(int editor_id, EditorMain EditMain) {
		super(editor_id, EditMain);
		this.texture_key_name = "bump_map";
		this.editorName = "bumpmap";
	}

}
